# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build maketing-apis`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t maketing-apis`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system maketing-apis && \
          adduser --system -G maketing-apis maketing-apis

COPY  maketing-apis
RUN chown -R maketing-apis:maketing-apis .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix maketing-apis --omit=dev -f install

CMD [ "node", "maketing-apis" ]
